# DebtIQ — Security & Access Posture

This document records the security baseline and the **owner-held** access
model introduced in Phase 5 of the integration program. It is a *posture and
plan* document: items marked `ARCH-REVIEW:` or `LEGAL-REVIEW:` are decisions
for the owner / qualified counsel and are **not** executed by the build agent.

> DebtIQ does not assert that it "is compliant", holds a credit licence, is an
> AML reporting entity, or is CDR-accredited. This document describes the
> product's security *features and configuration*, not a legal determination.

---

## 1. Secrets posture (owner-held)

- **All secrets live in a secrets manager the owner controls.** Today that is
  the Cloudflare Pages project's environment variables (`ANTHROPIC_API_KEY`
  server-only; `SUPABASE_URL` / `SUPABASE_ANON_KEY` public-by-design). See
  `BACKEND.md`.
- **The codebase reads from env / secret references only.** No key, password,
  token, or secret is hard-coded or generated by the application or the build
  agent. Invite tokens generated in demo mode are opaque, non-secret
  placeholders; real invites mint a server-side token and store only its
  **hash** (`invitations.token_hash` in migration `0002`).
- `ARCH-REVIEW:` Wiring a dedicated secret store (e.g. Cloudflare Secrets, a
  cloud KMS) beyond Pages env vars — owner action.
- `ARCH-REVIEW:` Owner super-admin account provisioning and any real auth
  account creation — owner action, never performed by the agent.

## 2. Access control / RBAC (Phase 5)

Roles, highest → lowest privilege: **Owner → Admin → Broker → Processor →
Read-only**. Deny-by-default: a capability is denied unless the role's
capability list explicitly grants it.

| Capability | Owner | Admin | Broker | Processor | Read-only |
|---|:--:|:--:|:--:|:--:|:--:|
| deal.create | ✓ | ✓ | ✓ | · | · |
| deal.edit | ✓ | ✓ | ✓ | ✓ | · |
| deal.delete | ✓ | ✓ | ✓ | · | · |
| deal.submit | ✓ | ✓ | ✓ | · | · |
| group.manage | ✓ | ✓ | ✓ | · | · |
| pack.generate | ✓ | ✓ | ✓ | ✓ | · |
| settlement.action | ✓ | ✓ | ✓ | · | · |
| settings.edit | ✓ | ✓ | · | · | · |
| member.invite | ✓ | ✓ | · | · | · |
| member.role.change | ✓ | ✓ | · | · | · |
| member.remove | ✓ | ✓ | · | · | · |
| pii.view | ✓ | ✓ | ✓ | ✓ | · |
| pii.export | ✓ | ✓ | · | · | · |
| audit.view | ✓ | ✓ | · | · | · |

**Owner primacy:** only an Owner can grant/revoke the Owner role; Owner cannot
be granted by invitation (must be transferred explicitly); at least one active
Owner must always exist (enforced in the app and by the `enforce_last_owner`
trigger in migration `0002`). Admins manage everyone **except** Owners.

**Demo vs live.** The front-end model (`state.access`, `can()`,
`requirePermission()`) is fully interactive in **demo mode** (in-memory +
`localStorage`, mirroring the `0002` tables). In **live mode**, until the
membership tables are wired, `can()` preserves today's per-user row-level
behaviour so nothing breaks.
- `ARCH-REVIEW:` Apply migration `0002_access_control.sql` and wire the app to
  read membership/role from Supabase (turns enforcement on in live mode).

## 3. Audit & sessions

- **Append-only auth/privilege log.** Every auth and privilege event (invite,
  role change, member removal, re-auth, permission-denied, PII export) is
  recorded. The `auth_audit_log` table has insert + select policies but **no
  update or delete policy** — history cannot be rewritten from the client.
- **Session timeout** (configurable; default 30 min idle) and **re-auth for
  sensitive actions** (role changes, member removal, PII export, settlement
  actions). Re-auth is cached for 5 minutes to avoid nagging.
- `ARCH-REVIEW:` Live re-auth must re-validate the Supabase session; server-side
  active-session listing and revocation need backend support.

## 4. Data access & PII

- **Record-level scoping.** RLS scopes every new table to active org members;
  migration `0002` adds a nullable `deals.org_id` for org-level scoping
  (additive — existing own-rows-only policy stays in force).
- **PII masking.** Email/phone are masked for roles without `pii.view`.
- **PII export is privileged and logged** (`pii.export` + re-auth + audit
  entry).
- `LEGAL-REVIEW:` APP 5 collection notices, data-retention/deletion controls,
  sub-processor DPAs (Anthropic, Supabase, Cloudflare), and the data-flow map
  are Phase 6 features / counsel decisions.

## 5. Security baseline (recommended — gated)

These are **recommendations**; applying anything that changes live response
behaviour is gated (it takes effect only on merge + deploy).

- **Transport:** HTTPS everywhere (Cloudflare Pages default). Supabase + the
  Anthropic proxy are TLS-only.
- **At rest:** Supabase (Postgres) encrypts data at rest by default.
- `ARCH-REVIEW:` **Security headers / CSP.** Recommended Cloudflare Pages
  `_headers` (not yet committed — review before enabling, as a strict CSP can
  break the CDN font/script loads currently used):

  ```
  /*
    Content-Security-Policy: default-src 'self'; script-src 'self' https://cdn.jsdelivr.net 'unsafe-inline'; style-src 'self' https://fonts.googleapis.com 'unsafe-inline'; font-src https://fonts.gstatic.com; connect-src 'self' https://*.supabase.co; img-src 'self' data:; frame-ancestors 'none'
    X-Frame-Options: DENY
    X-Content-Type-Options: nosniff
    Referrer-Policy: strict-origin-when-cross-origin
    Strict-Transport-Security: max-age=31536000; includeSubDomains
    Permissions-Policy: geolocation=(), microphone=(), camera=()
  ```
  Note the current inline scripts/styles require `'unsafe-inline'`; moving to
  hashed/nonce'd assets would harden this further (larger refactor).
- `ARCH-REVIEW:` **CORS.** `functions/api/_lib.js` sets
  `Access-Control-Allow-Origin: '*'`. Tightening to the deployed origin(s) is a
  live `/api/*` contract change → gated.
- `ARCH-REVIEW:` **Rate limiting** on auth + the AI proxy (Cloudflare WAF / rate
  rules) to limit credential stuffing and credit burn.
- **Input validation** is applied on every new Phase 5 surface (email format on
  invite, role enum checks, deny-by-default capability checks).
- **CSRF/session:** Supabase JWT in the `Authorization` header (not cookies) for
  `/api/*`; the proxy verifies the session before calling Anthropic.

## 6. Open review register (carried from Phase 0)

`LEGAL-REVIEW:` ACL/licensing status · AML reporting-entity status · trust-money
handling · PEXA/Sympli subscriber eligibility · CDR accreditation · ADM
disclosure wording · sub-processor DPAs.

`ARCH-REVIEW:` Apply `0001`/`0002` migrations · membership wiring · secret-store
wiring · owner-account provisioning · `_headers`/CSP · CORS tightening · rate
limiting · live re-auth + session revocation.
